import { StyleSheet } from 'react-native';

export const forgottenPasswordStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    backgroundColor: 'white',
    padding: 20
  },
  recoverButton: {
    marginVertical: 10,
    backgroundColor: '#6200ee'
  }
});